# Flower Delivery App

Monorepo from **Frontend** (React + Redux + Material UI) і **Backend** (Express + Mongoose).

## 📂 Structure

- `frontend/` – React-app
- `backend/` – Node.js API

## 🚀 Start

### Frontend

```bash
cd frontend
npm install
npm run dev

